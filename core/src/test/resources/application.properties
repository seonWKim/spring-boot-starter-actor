# Disable JVM shutdown hook registration in tests to prevent
# "Could not addJvmShutdownHook, due to: Shutdown in progress" warnings
# when Spring test contexts shut down concurrently during test teardown.
# The @PreDestroy hook in ActorConfiguration ensures proper shutdown.
spring.actor.pekko.coordinated-shutdown.run-by-jvm-shutdown-hook=off
