apiVersion: v1
kind: Service
metadata:
  name: chat-app-headless
  labels:
    app: chat-app
spec:
  selector:
    app: chat-app
  ports:
  - port: 2551
    targetPort: 2551
    name: pekko
  clusterIP: None  # This makes it a headless service
  publishNotReadyAddresses: true  # Important for cluster formation
